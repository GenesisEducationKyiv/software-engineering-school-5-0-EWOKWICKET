{
  "name": "subscription",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/apps/subscription/src/main",
    "build": "nest build",
    "lint": "eslint .",
    "format": "prettier --write .",
    "migrate": "yarn tscpaths src/database/infrastructure/scripts/run-migrations.ts",
    "migrate:test": "yarn tscpaths src/test/run-migrations.ts",
    "tscpaths": "ts-node -r tsconfig-paths/register"
  },
  "dependencies": {
    "@nestjs/mongoose": "^11.0.3",
    "mongoose": "^8.16.3"
  }
}
