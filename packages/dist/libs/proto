syntax = "proto3";

package weather;

message GetCurrentRequest {
    string city = 1;
}

message Weather {
    double temperature = 1;
    double humidity = 2;
    string description = 3;
} 

service WeatherService {
    rpc getCurrent (GetCurrentRequest) returns Weather;
}
