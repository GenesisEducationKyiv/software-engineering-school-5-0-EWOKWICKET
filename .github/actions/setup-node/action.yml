name: Setup Node
description: common setup for jobs

runs:
  using: 'composite'
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: '22.x'
        cache: yarn
        cache-dependency-path: packages/api/yarn.lock
